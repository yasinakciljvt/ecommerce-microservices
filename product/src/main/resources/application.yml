spring:
  application:
    name: product-service

  h2:
    console:
      enabled: true
    datasource:
      url: jdbc:h2:mem:test
    jpa:
      hibernate:
        ddl-auto: create
      show-sql: true


server:
  port: 8081

